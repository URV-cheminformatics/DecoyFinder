Dependencies:
- Core:
    - Python >= 2.5 (may work in Jython or IronPython if  using cinfony)
    - OpenBabel >= 2.3 (may be replaceable if using cinfony)
    - pybel OR cinfony

- GUI:
    - PySide >= 1.0 OR PyQt4 >= 4.6* (PyQt4 untested)
